---
name: templatecentral:shared-add-form
description: Use when adding a form with validation to a Next.js or Vite + React project — React Hook Form, Zod schema, and CustomFormField.
---

Read `AGENTS.md` to identify the stack, then run the corresponding command:

- **Next.js:** `cat "$HOME/.claude/plugins/marketplaces/templatecentral/skills/shared-add-form/nextjs.md"`
- **Vite + React:** `cat "$HOME/.claude/plugins/marketplaces/templatecentral/skills/shared-add-form/vite-react.md"`

After running the cat command, follow the loaded guide exactly.

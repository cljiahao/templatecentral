---
name: templatecentral:shared-add-page
description: Use when adding a new URL route or page with loading/error states to a Next.js or Vite + React project.
---

Read `AGENTS.md` to identify the stack, then run the corresponding command:

- **Next.js:** `cat "$HOME/.claude/plugins/marketplaces/templatecentral/skills/shared-add-page/nextjs.md"`
- **Vite + React:** `cat "$HOME/.claude/plugins/marketplaces/templatecentral/skills/shared-add-page/vite-react.md"`

After running the cat command, follow the loaded guide exactly.

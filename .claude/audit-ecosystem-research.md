---
last-scanned: 2026-06-29
expires-after-days: 30
---

# Ecosystem Research Cache

> Auto-generated by /tc-audit Step 0. Edit only to correct errors; the next full scan overwrites this file.

## Scan date: 2026-06-29

## Frameworks and runtimes

### Next.js
Current stable `16.2.9` (LTS line = `next@latest`); `16.3.0` is newest feature release (2026-06-26, opt-in). Floor `≥16.2.6` is still safe — May 2026 coordinated release patched 13 advisories (incl. RSC CVE-2026-23870) in 16.2.6. No new advisory since 2026-06-12. Optional: bump floor to `≥16.2.9` for LTS patches.

### NestJS + @nestjs/platform-fastify
Core `11.1.27`; platform-fastify `11.1.27` (bundles Fastify `5.8.5`). **ACTION (security): rules floor `@nestjs/platform-fastify ≥11.1.19` is BELOW the CVE-2026-54281 fix (trailing-slash auth-bypass, Critical, fixed 11.1.24).** Also CVE-2026-2293 (path-normalization), CVE-2026-33011 (HEAD→GET). Raise floor to `≥11.1.24` (ideally `≥11.1.27`). Note: scaffold `config-files.md` already pins `^11.1.24` (generated projects resolve safe); the stale floor is in `.claude/rules/nestjs.md`.

### FastAPI + Starlette
FastAPI `0.138.1` (declares `starlette>=0.46.0`, `pydantic>=2.9.0`); Starlette `1.3.1`; Pydantic `2.13.4`; Uvicorn `0.49.0`. Floors safe. BadHost host-header auth-bypass CVE-2026-48710 affected ≤1.0.0, fixed 1.0.1 — floor `≥1.0.1` clears it. **Doc fix:** rules note says "current stable 1.2.1" → should read `1.3.1`. Optional: bump FastAPI floor `0.136`→`0.138`. **Separate finding (sweep):** FastAPI deps are `pip install`ed unpinned then frozen — floors documented in rules are not applied at install.

### pnpm
Current `11.9.0`. Major 11 correct. pnpm 11 defaults: `minimumReleaseAge=1440` (can block fresh CI installs), `blockExoticSubdeps=true`, requires Node ≥22, pure ESM. Verify scaffolded configs account for `minimumReleaseAge`.

### Node / TypeScript / Tailwind / Python
Node `26.4.0` Current, `24.x` Active LTS — floor `≥24` correct (do not raise to 26 until Oct 2026 LTS). TypeScript `6.0.3` (6.0 changed ~9 defaults: strict-on, ESM-default) — ensure scaffolded tsconfig accounts for these. Tailwind `4.3.1`. Python `3.13.14` (maintenance) — floor safe; 3.14 is current feature line.

## Libraries

### Drizzle ORM
**No GA.** `latest`=`0.45.2` (v0.x); `rc`=`1.0.0-rc.4` (2026-06-27). RC-pin strategy remains correct (nothing stable to move to). Pin `1.0.0-rc.3` is one RC behind (`@rc` tag now resolves to rc.4). **Bug (sweep):** `add/database/typescript/nestjs-drizzle.md` installs `pnpm add drizzle-orm` (no `@rc`) → resolves v0.45, not v1; Next.js sibling correctly uses `@rc`. Optional currency: bump rules pin rc.3→rc.4.

### better-auth
`1.6.22` (`latest`). Breaking in 1.6: token introspection / `/oauth2/userinfo` behavior; **OIDC Provider plugin being deprecated → OAuth Provider plugin** (PKCE required by default, scope config changed). If `add/auth/nextjs.md` uses the OIDC Provider plugin, plan migration.

### Zod
`4.4.3`. `z.flattenError()` / `z.url()` current; `.flatten()` / `z.string().url()` deprecated (work in v4, removed v5). Assumptions accurate.

### TanStack Query
`5.101.2`. **No React v6** — firmly v5 ("v6" online = Svelte adapter only). `isInitialLoading` deprecated; prefer `isPending`. Keep `5` pin.

### Vite + @vitejs/plugin-react
Vite `8.1.0`; plugin-react `6.0.3` (Oxc, no Babel). Current.

### React Router
**MAJOR DRIFT: `8.0.1` is current; scaffold pins `7`.** Breaking: ESM-only; `react-router-dom` re-export removed (import from `react-router` + `react-router/dom`); `future.v8_*` flags removed (now default); meta `data`→`loaderData`; Node ES2022 baseline. Migration "minimal" if v7 future flags were adopted. Affects vite-react scaffold + add (page/feature/form/error-handling).

### React
`19.2.7` = current latest, floor at stable. RSC DoS/source-exposure chain fixed in 19.2.4. No change needed.

### argon2 / argon2-cffi
argon2 (node) `0.44.0`; argon2-cffi `25.1.0`. OWASP Argon2id params unchanged (m≥19 MiB, t≥2, p=1). Confirm scaffold params.

### @nestjs/throttler / slowapi
throttler `6.5.0`; slowapi `0.1.10`. Current, no API change.

### pymongo / beanie / python-json-logger
pymongo `4.17.0` (floor `≥4.13` valid; AsyncMongoClient GA); beanie `2.1.0` (floor `≥2.0` valid; Motor past EOL 2026-05-14 — ensure no residual Motor); python-json-logger `4.1.0` (floor `≥4.0` valid).

## Security standards

### OWASP Top 10 (web)
**DRIFT: OWASP Top 10:2025 is now current (finalized Jan 2026).** New: A02 Security Misconfiguration (up to #2), **A03 Software Supply Chain Failures (NEW)**, **A10 Mishandling of Exceptional Conditions (NEW)**; A01 Broken Access Control still #1 (SSRF absorbed). Update any "2021" references in skills/audit.

### OWASP LLM Top 10
`2025` edition (publ. 2024-11-17) still current — no 2026 successor. LLM01 Prompt Injection → LLM10 Unbounded Consumption. No drift.

### OWASP Top 10 for Agentic Applications
**DRIFT: 2026 edition FINAL (released 2025-12-09).** ASI prefix: ASI01 Agent Goal Hijack, ASI02 Tool Misuse, ASI03 Identity & Privilege Abuse, ASI04 Agentic Supply Chain, ASI05 Unexpected Code Execution, ASI06 Memory/Context Poisoning, ASI07 Insecure Inter-Agent Comms, ASI08 Cascading Failures, ASI09 Human-Agent Trust Exploitation, ASI10 Rogue Agents. Adopt as first-class reference in `add/ai-security` + audit Tier C.

### NIST SP 800-63B
**DRIFT: Rev 4 FINAL (Aug 2025).** Min 8 chars (15 when sole authenticator), support ≥64; **SHALL NOT require periodic rotation** (unless compromise); **SHALL NOT impose composition rules**; blocklist check required. Check `standards/validation-patterns/*` + auth skills (rules already say password min ≥12 — aligns with intent).

### AWS Responsible AI Lens / AIDLC
**NEW: AWS Well-Architected Responsible AI Lens launched (re:Invent Nov 2025).** Ten dimensions (controllability, privacy, security, safety, veracity, robustness, fairness, explainability, transparency, governance). AWS "AI-DLC" = Inception→Construction→Operations with human approval gates. Reference in `add/ai-security`.

## Claude Code Harness Engineering
- **Hook events:** official docs now list ~30 events (Setup, UserPromptExpansion, PostToolBatch, PermissionRequest/Denied, SubagentStart, TaskCreated/Completed, StopFailure, Pre/PostCompact, SessionEnd, etc.). templateCentral's 7 (PreToolUse, UserPromptSubmit, PostToolUse, PostToolUseFailure, Stop, SubagentStop, SessionStart) are all still valid — but "the current set/count is 7" framing is stale.
- **Hook handler types:** command/http/mcp_tool/prompt/agent — all current; `agent` experimental. No drift.
- **settings.json:** `skillListingBudgetFraction` (default 0.01), `skillListingMaxDescChars` (default 1536) current; `worktree.*` expanded; sandbox `allowUnsandboxedCommands` (default true) added.
- **Skill scoping:** Managed > User > Project for skills; user STILL overrides project on collision (templateCentral's distinct-naming guidance correct). NOTE: subagent precedence is inverse (Project > User) — don't state a single unified order.
- **Stop hook "block cap 8":** **no official numeric cap exists** — the only guard is `stop_hook_active` (hook must exit 0 when it sees it; templateCentral does this correctly). Reword/drop the "cap was 8" claim in audit Step 3H.
- **Compaction recovery:** SessionStart (`source: compact`) remains the documented mechanism; PostCompact cannot inject context. templateCentral correct.

### Community consensus & team recommendations
- `RECOMMENDED` (official docs): hook-event set has grown to ~30; `stop_hook_active` is the sole Stop loop-guard (no numeric cap); SessionStart-for-compaction correct; 5 handler types current.
- `RECOMMENDED` (status change): AGENTS.md is now a Linux Foundation open standard under the Agentic AI Foundation (AAIF, formed Dec 2025; ~180 members incl. Anthropic, AWS, Google, OpenAI). Worth noting in AGENTS.md provenance.
- `CONSENSUS`: skill-listing budget fields (`skillListingBudgetFraction` 0.01 default) — templateCentral sets 0.02, fine.
